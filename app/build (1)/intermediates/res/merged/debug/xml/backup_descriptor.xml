<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude the database as it's role is just to the cache the data -->
    <exclude
        domain="database"
        path="appDBncache.db" />
    <!-- Exclude specific shared preferences that contain GCM registration Id -->
</full-backup-content>
